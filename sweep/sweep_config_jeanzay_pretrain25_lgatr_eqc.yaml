cluster:
  account: itg@v100
  auto_submit: false
  cpus_per_task: 8
  partition: gpu_p2
  request_gpus: 1
  scheduler: slurm
  time: "02:00:00"
dyhpo:
  n_candidates: 300
  n_startup: 8
  seed: 42
  total_budget: 10000
fidelity_schedule:
  # Equal training COMPUTE to LLoCa's 15000-step pretrain25 run: L-GATr (full)
  # measured 9.992e11 FLOP/step vs LLoCa 4.878e10 (20.5x) in one shared profiling
  # run (batchsize 1024). Matching total FLOPs: 15000 * 4.878e10 / 9.992e11 = 732.
  # (Fewer steps than the equal-wall-time 1151, since L-GATr utilises the V100
  # better than its FLOP count alone implies -- 20.5x FLOPs but only 13x wall time.)
  t_steps:
  - 732
fixed_params:
  # --- identical 25-process recipe data to pretrain25 (the LLoCa baseline) ---
  data.source: recipes
  data.processes_file: /lustre/fswork/projects/rech/itg/ulm49ia/Foundational_Amplitudes/recipes/pretrain25.yaml
  data.preprocess_per_dataset: 'true'
  data.require_cache: 'true'
  # 500k/process (12.5M pool): identical to the LLoCa pretrain25 baseline, so every
  # architecture sees the same data pool and the comparison's only controlled axis is
  # t_steps. At equal compute L-GATr sees ~0.75M samples (732 x 1024) << 12.5M, i.e.
  # no repetition. val/test come from the separate frozen eval pool (eval_subsample
  # 20k below), so this knob does not affect the comparison metric.
  data.train_subsample: 500000
  data.eval_subsample: 20000
  data.seed: 42
  data.data_path: /lustre/fswork/projects/rech/itg/ulm49ia/Foundational_Amplitudes/data/
  # --- model: full L-GATr muP, ~1.64M params (matched to LLoCa's 1.61M) ---
  model: lgatr_mup
  model.net.num_blocks: 8
  model.net.num_heads: 2            # fixed structure (muP width axis = hidden channels)
  model.net.hidden_mv_channels: 22
  model.net.hidden_s_channels: 22
  evaluation.batchsize: 8192
  plot: 'true'
  seed: 42
  training.batchsize: 1024
  training.get_ID: 'false'
  training.loss_aggregation: geometric_mean
  training.regularization: L2
  training.save_intermediate: 'false'
  training.scheduler: CosineAnnealingLR
  # 0.1 (not pretrain25's 0.02): geometric forward is ~15x slower, so 50 validations
  # would dominate walltime; ~10 validations still tracks the best checkpoint.
  training.validate_frac: 0.1
n_trials: 20
paths:
  project_dir: /lustre/fswork/projects/rech/itg/ulm49ia/Foundational_Amplitudes
  setup_commands:
  - module load anaconda-py3/2023.09
  - conda activate /lustre/fswork/projects/rech/itg/ulm49ia/conda/envs/foundational
  sweep_dir: /lustre/fswork/projects/rech/itg/ulm49ia/Foundational_Amplitudes/sweeps/pretrain25_lgatr_eqc
range_extension:
  enabled: false
search_space:
- {name: training.lr,                     type: float_log,     low: 5.0e-4, high: 1.0e-2}
- {name: training.regularization_lambda,  type: float_log,     low: 1.0e-11, high: 1.0e-6}
- {name: training.cosanneal_warmup_frac,  type: float_uniform, low: 0.0,    high: 0.2}
- {name: training.cosanneal_eta_min,      type: float_log,     low: 1.0e-11, high: 1.0e-6}
- {name: training.ema_decay,              type: float_uniform, low: 0.9,    high: 0.9999}
- {name: training.sampler_alpha_ema,      type: float_uniform, low: 0.3,    high: 0.95}
- {name: training.sampler_min_alpha_frac, type: float_uniform, low: 0.05,   high: 0.5}
sweep_name: pretrain25_lgatr_eqc
